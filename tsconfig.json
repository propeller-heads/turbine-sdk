{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        // This configuration is used for local development and type checking
        "baseUrl": "./src",
        "paths": {
            "~turbine/*": ["./*"]
        }
    },
    "references": [
        { "path": "./scripts" }
    ]
}
